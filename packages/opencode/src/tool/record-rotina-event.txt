Records that this task used a repeatable work routine (a rotina), so the recurrence report can flag the SAME routine appearing across ≥5 distinct tasks as a candidate for a new tool/skill. Called by the workflow-executor, in the same turn as the execution-log write, for each reusable routine used in the cycle. Never called by the avaliador (reviewer blindness). One call per (task, rotina) is enough — recurrence counts distinct tasks, not how many times it was called within a task.