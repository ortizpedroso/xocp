export const HEADROOM_PINNED_VERSION = "0.37.0"

export const HEADROOM_DEFAULT_PORT = 8787

export const HEADROOM_DEFAULT_BASE_URL = `http://127.0.0.1:${HEADROOM_DEFAULT_PORT}/v1`

// Installed via `uv tool run --from headroom-ai[proxy]==<version>` (isolated, no global pip install).
